const DEV_DB = 'mongodb://localhost:27017/takeoffstaffdb';

export const URL_DB = DEV_DB;
export const JWT_SECRET = 'dev-secret';

export const SETUP_DB = {
  useNewUrlParser: true,
  useUnifiedTopology: true
};
